import React from "react";

const NoNotificationMessage = ({}) => {
  return (
    <div style={{ marginTop: "30%", textAlign: "center" }}>
      <p>You don't have any new notifications.</p>
    </div>
  );
};

export default NoNotificationMessage;
